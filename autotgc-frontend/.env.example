# Base URL for the backend API.
# Leave EMPTY for same-origin (served behind nginx at the same host as the API).
# During `vite dev` the proxy in vite.config.ts forwards /api to the backend,
# so this can also stay empty in development.
VITE_API_BASE=
